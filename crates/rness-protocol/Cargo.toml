[package]
name = "rness-protocol"
description = "Wire types only: session events, live frames, branch lineage, SessionService API. No logic, no tokio."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
ulid.workspace = true
jiff.workspace = true
